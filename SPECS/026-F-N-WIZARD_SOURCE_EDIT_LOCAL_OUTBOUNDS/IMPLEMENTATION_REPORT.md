# Отчёт: 026-F-N-WIZARD_SOURCE_EDIT_LOCAL_OUTBOUNDS

**Статус:** не реализовано.

**Дата:** —

## Кратко

— (заполнить: файлы; **`exclude`/`expose`** на генерации; без мутации JSON **`addOutbounds`**; **`expose`** + **`filters`** / синтетика (**SPEC §5**); дедуп/порядок; маркеры **WIZARD:**; миграция; **docs/ParserConfig.md** по **PLAN §6**).

## Проверки

— (`go test`, ручные сценарии)
